name: Conventional Commits

on:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate pull request title
        env:
          PULL_REQUEST_TITLE: ${{ github.event.pull_request.title }}
        run: |
          conventional_commit='^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\([^)]+\))?!?: .+$'
          if [[ ! "$PULL_REQUEST_TITLE" =~ $conventional_commit ]]; then
            echo "Pull request title must follow Conventional Commits."
            exit 1
          fi
